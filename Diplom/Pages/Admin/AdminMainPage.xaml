<Page x:Class="Diplom.Pages.Admin.AdminMainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Diplom.Pages.Admin"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage" IsVisibleChanged="Page_IsVisibleChanged">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" HorizontalAlignment="Left" Orientation="Horizontal">
            <Button x:Name="backBut" Content="Назад" Click="backBut_Click"/>
            <Button x:Name="settingBut" Content="Подключение" Click="SettingsButton_Click"/>
        </StackPanel>
        
        <ScrollViewer Grid.Row="1">
            <TabControl>
                <TabItem>
                    <TabItem.Header>
                        <TextBlock Text="Мои тесты"/>
                    </TabItem.Header>
                    <TabItem.Content>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>
                            <Button Content="Добавить тест" Width="150" HorizontalAlignment="Left" Margin="10" Click="AddTestButton_Click" Grid.Row="1"/>
                            <ListView x:Name="MyTestListView" ItemsSource="{Binding MyTests}" Grid.Row="0">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="1*"/>
                                                <RowDefinition Height="1*"/>
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="3*"/>
                                                <ColumnDefinition Width="1*"/>
                                                <ColumnDefinition Width="1*"/>
                                                <ColumnDefinition Width="1*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Name}" Grid.Column="0"/>
                                            <Button Content="Результаты" Grid.Column="1" Click="ResultBut_Click"/>
                                            <Button Content="Редактировать" Grid.Column="2" Click="EditTestButton_Click"/>
                                            <Button Content="Удалить" Grid.Column="3" Click="DeleteTestButton_Click"/>
                                            <Expander Grid.Row="1" Grid.ColumnSpan="4">
                                                <Expander.Header>
                                                    <TextBlock Text="Описание"/>
                                                </Expander.Header>
                                                <Expander.Content>
                                                    <TextBlock Text="{Binding Description}"/>
                                                </Expander.Content>
                                            </Expander>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Grid>
                    </TabItem.Content>
                </TabItem>
                <TabItem>
                    <TabItem.Header>
                        <TextBlock Text="Студенты"/>
                    </TabItem.Header>
                    <TabItem.Content>
                        <DataGrid x:Name="StudentTable" AutoGenerateColumns="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Фамилия" Binding="{Binding SName}" Width="1*"/>
                                <DataGridTextColumn Header="Имя" Binding="{Binding FName}" Width="1*"/>
                                <DataGridTextColumn Header="Отчество" Binding="{Binding PName}" Width="1*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </TabItem.Content>
                </TabItem>
            </TabControl>
        </ScrollViewer>
    </Grid>
</Page>
