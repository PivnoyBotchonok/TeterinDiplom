<Page x:Class="Diplom.Pages.Admin.CreateTest"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Diplom.Pages.Admin"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CreateTest">

    <ScrollViewer>
        <StackPanel Margin="20">
            <Label Content="Название теста:" />
            <TextBox x:Name="TestNameTextBox" Margin="0,0,0,10" />

            <Label Content="Описание теста:" />
            <TextBox x:Name="TestDescriptionTextBox" Margin="0,0,0,10" Height="100" TextWrapping="Wrap" AcceptsReturn="True" />

            <Label Content="Вопросы:" />
            <ListView x:Name="QuestionsListView" Margin="0,0,0,10" HorizontalContentAlignment="Stretch">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Вопрос" Width="500">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Vertical">
                                        <Label Content="Текст вопроса"/>
                                        <TextBox Text="{Binding Text}" Width="300" Margin="0,0,0,5" HorizontalAlignment="Left"/>

                                        <ItemsControl ItemsSource="{Binding Answers}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel />
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>

                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBox Text="{Binding Text}" Width="200" Margin="0,0,5,0"/>
                                                        <CheckBox Content="Правильный ответ" IsChecked="{Binding IsCorrect}"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>

                                        <Button Content="Добавить ответ" Click="AddAnswerButton_Click" />
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>

            <Button Content="Добавить вопрос" Click="AddQuestionButton_Click" Margin="0,0,0,10" />
            <Button Content="Сохранить тест" Click="SaveTestButton_Click" />
        </StackPanel>
    </ScrollViewer>
</Page>
